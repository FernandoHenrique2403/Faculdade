algoritmo (cilindro);
	real h,r,volume,areaBase;
	inicio
	escreva ("Digite o o valor em cm de h: ");
	leia (h);
	escreva ("Digite o o valor em cm de r: ");
	leia (r);
	areaBase= 3,14*(r*r);
	volume = h*3,14*(r*r);
	escreva("A área da Base do cilindro é: " areaBase + "cm²" + "e o Volume do cilindro é: " + volume +"cm³.");
	fim
	