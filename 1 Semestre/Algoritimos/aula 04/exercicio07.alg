algoritmo quadro
    real x,y,z
    início
    escreva("Digite a altura do quadro")
    leia(x)
    escreva("digite a altura da escada")
    leia(z)
    y = raiz(x^2-z^2)
    escreva("a distancia da escada para a parede é de : " + y)
        
    fim